# DNV — Materials Informatics Pipeline
# Python >= 3.11
# Install: uv pip install -r requirements.txt

# ── Core dependencies ────────────────────────────────────────────────────
numpy>=2.0
pandas>=2.0
scipy>=1.12
scikit-learn>=1.4
matplotlib>=3.9
PySide6>=6.6

# ── Optional but recommended ─────────────────────────────────────────────
seaborn>=0.13               # enhanced visualisations (gui_dlg_viz_plot, gui_dlg_cor_plot)
xgboost>=2.0                # XGBoost ML method (ml_engine)
lightgbm>=4.0               # LightGBM ML method (ml_engine)
torch>=2.0                  # PyTorch MLP (ml_engine, fi_engine deep attribution)

# ── Correlation module optional dependencies (cor_engine) ────────────────
# Each gated behind a try/except ImportError in cor_engine.py; installing
# them enables the 3 correlation methods listed alongside.  Known-good
# versions against which cor_engine is tested are pinned as minimum bounds.
statsmodels>=0.14           # Granger causality (granger_matrix).  API change:
                            #   statsmodels >=0.14 renamed maxlags→maxlag.
pyinform>=0.2               # Transfer Entropy (transfer_entropy_matrix).
                            # Requires integer-coded columns; caller bins first.
pgmpy>=1.0                  # Bayesian Network structure learning
                            # (bayesian_network_edges).  pgmpy >=1.0 moved
                            # K2Score→K2 under pgmpy.structure_score and
                            # selects score family from pandas 'category'
                            # dtype; cor_engine handles both the old and
                            # new APIs.
networkx>=3.0               # DAG data structure used by pgmpy
